#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
时间线相关提示词
"""

# ==================== 时间线生成提示词 ====================

TIMELINE_GENERATION_PROMPT = '''你是一位专业的小说时间线规划师，擅长根据世界观设定梳理宏观时间脉络。
请根据以下小说的世界观设定，生成故事的时间线概述：

小说标题：{project_title}

世界观基础设定：
{worldview_setting}

世界历史背景：
{worldview_history}

世界基础信息：
{worldview_foundation}

用户额外要求：{extra_prompt}

时间线要求：
1. 基于世界观设定，梳理出故事的宏观时间脉络，侧重时代划分和阶段概述
2. 每个事件标注对应的时间段，使用结构化时间字段：era_unit（纪年单位，如"公元"、"洪武"、"贞观"等）、start_year/start_month（起始年月）、end_year/end_month（结束年月），月份为0表示未指定，年份0表示元年（注意：年份和月份必须是纯数字，禁止输出"元年"等文字，0就写0）
3. 事件标题应概括该时间段的时代特征或核心主题
4. 内容描述应为概述性质，用2-4句话概括该时间段的时代背景、主要走向和关键转折，避免展开具体情节细节
5. 重点关注世界观层面的重大变迁（如文明兴衰、种族迁徙、力量体系演变等）
6. 可以适当推断合理的历史发展
7. 时间线应涵盖从世界观古代到未来的完整跨度

输出格式要求（重要）：
1. 只输出分隔符包裹的 JSON 内容，不要输出任何开场白、介绍、解释或结束语
2. JSON 必须使用标准英文双引号（"），禁止使用中文引号（" "）或其它特殊引号
3. 每一项用 ════ITEM_START════ 和 ════ITEM_END════ 包裹
4. 所有项输出完毕后直接结束，不要输出"已为您生成"之类的总结
5. 字符串值中禁止使用未转义的双引号，如需引号请用单引号

格式示例：
════ITEM_START════
{{"era_unit": "纪元", "start_year": 1, "start_month": 0, "end_year": 100, "end_month": 0, "title": "创世纪元", "content": "世界初诞生的混沌时期，诸神创造大陆与初代种族。万物遵循原始法则运转，智慧文明尚未萌芽。"}}
════ITEM_END════

════ITEM_START════
{{"era_unit": "纪元", "start_year": 101, "start_month": 0, "end_year": 300, "end_month": 0, "title": "诸神之战", "content": "诸神因信仰与领地爆发全面战争，世界格局剧变。部分神陨落，种族被迫迁徙，旧秩序崩塌。"}}
════ITEM_END════

如果有需要确认的问题，请使用以下格式（可放在最后）：
════QUESTION_START════
这是引导问题内容
选项：
- 选项1
- 选项2
════QUESTION_END════

再次强调：不要输出任何介绍文字、说明文字或总结文字，只输出上述格式的内容。'''


TIMELINE_INCREMENTAL_PROMPT_BAK = '''
你是一位时间线助手，负责根据用户的需求对时间线事件进行增删、优化。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
输入
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 小说标题：{project_title}
- 世界观摘要：{outline}
- 当前时间线（仅供定位参考）：{current_timeline}
- 用户需求：{user_input}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
规则
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【add】新增事件
- content 根据用户需求结合世界观自行生成

【modify】修改已有事件
- 必含字段：operation / match_title（原事件完整标题）/ title / content（era_unit/start_year/start_month/end_year/end_month 按场景决定）
- 先从 current_timeline 定位目标事件，按场景只改指定字段，其余逐字照抄：

  ┌──────────────────────┬──────────────────────────────────────────────────────────┐
  │ 场景                  │ 规则                                                       │
  ├──────────────────────┼──────────────────────────────────────────────────────────┤
  │ 仅改标题              │ title→新值，content/时间 逐字照抄                          │
  │ 仅改时间              │ era_unit/start_year/start_month/end_year/end_month→新值，  │
  │                       │ 标题/内容照抄                                              │
  │ 仅优化内容            │ 可调措辞语序，核心信息100%保留                              │
  │ 改标题 + 时间         │ 仅动 title + era_unit/start_year/start_month/              │
  │                       │ end_year/end_month                                         │
  │ 改标题 + 内容         │ 仅动 title + content 被提及部分                             │
  └──────────────────────┴──────────────────────────────────────────────────────────┘

【delete】删除已有事件
- 必含字段：operation / match_title（原事件完整标题），不输出其他字段

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
注意事项
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 仅处理用户指定的事件，其余事件原样保留。输出条目数 = 用户要求的变更数，禁止凭空新增。
2. 标题与内容互不联动：只改标题时 content 逐字照抄，不因新标题语义不同就去改写内容。
3. 优化内容时：可删减冗余、调整语序、扩充表达，但核心人物/地点/时间/数字/行为步骤/因果链必须全部保留，禁止概括式压缩为摘要。
4. 用户没说要改的字段 = 原样复制，绝不擅自改动。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
输出格式约束
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 只输出标准 JSON，禁止任何解释、前言、备注、总结。JSON 统一使用英文双引号。
- 单条变更用 ════ITEM_START════ / ════ITEM_END════ 包裹；信息不足时改用提问格式。
- 字符串值中禁止使用未转义的双引号，如需引号请用单引号

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
示例
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

add / modify：
════ITEM_START════
{{"operation": "add|modify", "match_title": "原事件标题（modify必填）", "era_unit": "纪年单位", "start_year": 0, "start_month": 0, "end_year": 0, "end_month": 0, "title": "事件标题", "content": "事件内容"}}
════ITEM_END════

delete：
════ITEM_START════
{{"operation": "delete", "match_title": "原事件标题"}}
════ITEM_END════

信息不足：
════QUESTION_START════
需要确认的问题
选项：
- 选项1
- 选项2
════QUESTION_END════
'''

TIMELINE_INCREMENTAL_PROMPT = '''
你是一位时间线助手，根据小说标题、世界观摘要、当前时间线和用户需求对事件进行操作。

输入：
小说标题：{project_title}
世界观摘要：{outline}
当前时间线：{current_timeline}
用户需求：{user_input}

操作：
- add：新增事件
- modify：修改已有事件
- delete：删除已有事件

输出格式：
仅输出以下格式的内容，用 ════ITEM_START════ / ════ITEM_END════ 包裹，禁止输出任何其他文字。JSON 使用英文双引号。
字符串值中禁止使用未转义的双引号，如需引号请用单引号。

add / modify：
════ITEM_START════
{{"operation": "add|modify", "match_title": "原事件标题（modify 必填）", "era_unit": "纪年单位", "start_year": 起始年份, "start_month": 起始月份, "end_year": 结束年份, "end_month": 结束月份, "title": "事件标题", "content": "事件内容"}}
════ITEM_END════

delete：
════ITEM_START════
{{"operation": "delete", "match_title": "原事件标题"}}
════ITEM_END════

信息不足无法处理时：
════QUESTION_START════
问题内容
选项
════QUESTION_END════
'''

TIMELINE_MERGE_PROMPT = '''请将以下多个时间线事件合并为一个事件，自动生成合并后的标题和描述。

小说标题：{project_title}

世界观设定摘要：{outline}

待合并的时间线事件：
{events_to_merge}

合并要求：
1. 合并后的标题要能概括所有被合并事件的核心时代特征
2. 描述保持概述性质，用2-4句话概括时代背景和主要走向
3. 时间范围取最早的开始时间到最晚的结束时间，era_unit取被合并事件中最主要的纪年单位

请严格按以下 JSON 格式输出，不要输出其他内容：
{{"era_unit": "纪年单位", "start_year": 起始年份, "start_month": 起始月份, "end_year": 结束年份, "end_month": 结束月份, "title": "合并后的标题", "description": "合并后的描述"}}'''


DESCRIPTION_OPTIMIZE_PROMPT = '''
你是一位专业的小说编辑。你的任务是润色优化给定时间线事件的描述，而非重写或扩展。

小说标题：{project_title}

世界观设定摘要：{outline}

事件标题：{title}

时间范围：{time_range}

当前描述：{content}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️ 优化前必读：核心约束（违反即失败）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
你的职责是"润色表达"，而非"改写内容"。

必须遵守：
1. 只优化文字表达，使概述更加精炼、概括性强
2. 保留原文所有关键信息点，不得遗漏
3. 不得添加原文中未提及的新信息、新事件或新转折
4. 不得改变原文的核心含义和叙事方向
5. 用2-4句话概括，突出世界观层面的重大变迁
6. 保持与世界观看似的一致性

禁止行为（常见错误）：
- ❌ 原文说"诸神爆发战争"，你改写为"诸神爆发战争，凡人趁机崛起建立新秩序"（添加了原文没有的"凡人崛起"）
  → 正确：只润色表达，如"诸神因信仰与领地爆发全面战争"
- ❌ 原文包含3个关键信息点，优化后只剩1个（遗漏了原文信息）
  → 正确：保留所有关键信息点，仅优化表达方式
- ❌ 原文是概述性质，你展开了具体情节细节
  → 正确：保持概述性质，不展开细节

自检规则：输出前对比原文，确认没有遗漏关键信息、没有添加原文未提及的内容。
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

输出格式：
请直接输出优化后的概述内容，不要输出任何开场白、解释或结束语。'''


SINGLE_ITEM_OPTIMIZE_PROMPT = '''
你是一位专业的小说时间线规划师。你的任务是优化给定时间线事件的表达，而非重写或扩展内容。

小说标题：{project_title}

世界观设定摘要：{outline}

当前事件：
标题：{title}
时间范围：{time_range}
描述：{content}

{context_section}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️ 优化前必读：核心约束（违反即失败）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
你的职责是"润色优化"，而非"重写扩展"。

必须遵守：
1. 标题优化：使标题更精炼地概括时代特征，但不得改变原事件的核心主题
2. 时间范围：
   - 年月字段含义：year=0表示"元年"（如洪武元年），month=0表示"整年"（不指定月份）
   - era_unit只写纪年名称（如"炎兴"、"洪武"、"公元"），禁止包含"元年"或"年"后缀
   - 如果原事件的start_year/start_month/end_year/end_month全为0，则必须根据世界观和描述内容推断合理的时间范围并填写，不得保留全0
   - 如果原事件已有时间范围，仅在明显不合理时微调，不得无故变更
3. 描述优化：润色文字表达，必须逐条保留原文所有关键信息点，不得遗漏任何核心内容。优化是让表达更流畅，而不是压缩或删减
4. 不得添加原文中未提及的新信息、新事件或新转折
5. 不得改变原文的核心含义和叙事方向
6. 与相邻事件保持时间连续性和逻辑连贯性，但不得修改相邻事件
7. 仅优化当前事件，绝对不要输出其他事件
8. 年份和月份必须是纯数字（整数），禁止输出"元年"等文字，0就写0

禁止行为（常见错误）：
- ❌ 原标题是"诸神之战"，你改为"诸神之战与凡人崛起"（添加了原文没有的主题）
  → 正确：只精炼表达，如"诸神之战"或"诸神纷争"
- ❌ 原描述说"诸神爆发战争"，你改写为"诸神爆发战争，凡人趁机崛起建立新秩序"（添加了原文没有的内容）
  → 正确：只润色表达，如"诸神因信仰与领地爆发全面战争"
- ❌ 原时间范围是"纪元101年-纪元300年"，你改为"纪元50年-纪元350年"（无故扩大范围）
  → 正确：保持原时间范围，除非有明显逻辑错误
- ❌ 原文包含3个关键信息点，优化后只剩1个（遗漏了原文信息）
  → 正确：保留所有关键信息点，仅优化表达方式
- ❌ 同时输出了相邻事件的优化结果
  → 正确：只输出当前这1个事件的优化结果
- ❌ 原描述有200字，优化后压缩到50字（过度简化导致核心内容丢失）
  → 正确：优化后字数应与原文相近，至少保留原文80%以上的内容量
- ❌ 原事件时间全为0，优化后仍然全为0（未推断时间范围）
  → 正确：根据世界观和描述推断合理年月，如era_unit="洪武", start_year=1, end_year=3

自检规则：
1. 输出前对比原文，确认没有遗漏关键信息、没有添加原文未提及的内容
2. 确认只输出了1个事件，没有输出相邻事件
3. 如果原文时间全为0，确认已推断并填写了合理的时间范围
4. 确认优化后的内容量不少于原文的80%，核心信息全部保留
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【输出格式】（严格遵守，违反即失败）
1. 必须输出完整的 JSON 对象，包含所有字段（era_unit、start_year、start_month、end_year、end_month、title、content），即使某字段未修改也必须输出原值
2. 只输出一个 JSON 对象，不要输出任何开场白、介绍、解释或结束语
3. JSON 必须使用标准英文双引号（"），禁止使用中文引号（" "）或其它特殊引号
4. 禁止只输出纯文本内容，必须输出完整的 JSON 对象
5. 字符串值中禁止使用未转义的双引号，如需引号请用单引号

格式示例：
{{"era_unit": "优化后的纪年单位", "start_year": 1, "start_month": 0, "end_year": 100, "end_month": 0, "title": "优化后的标题", "content": "优化后的概述性描述..."}}'''


TIMELINE_CHECK_PROMPT = '''
你是一位专业的时间线审校编辑，负责检查小说时间线中的问题。

【输入】
小说标题：{project_title}
世界观设定摘要：
{outline}
当前时间线事件列表：
{current_timeline}

时间格式说明：事件时间使用结构化字段表示，如"纪元=炎兴, 起始年=0, 起始月=11, 结束年=0, 结束月=11"。其中起始年=0表示该纪元的元年（第一年），起始年=1表示该纪元的第二年，以此类推。同一纪元下，起始年=0和起始年=1是不同年份，0在前1在后。

【职责】
检查时间线事件是否存在以下4类问题：

1. 时间范围不合理（unreasonable）：单个事件的时间范围与描述内容不匹配
   - 小事横跨数年（如一场宴会跨越3年）
   - 大事时间过短（如文明兴衰仅1个月）
   - 注意：奇幻世界中一个纪元长达千年是合理的，需结合描述内容判断

2. 时间冲突（conflict）：多个事件时间重叠但不合理
   - 同一地点同一时间发生逻辑上不可能并存的事件
   - 注意：时间重叠本身是允许的，只有逻辑上矛盾才算冲突

3. 逻辑矛盾（contradiction）：事件描述之间存在前后矛盾
   - A事件说某文明毁灭，B事件又引用该文明
   - A事件说某角色已死，B事件中该角色仍在行动

4. 重复事件（duplicate）：标题或描述高度相似的事件

【注意事项】
- 时间线允许有缺口，事件之间不需要连续，不得将缺口判定为问题
- 同一对事件只输出一条问题；若同时存在多种问题，按严重程度取最严重的一条，优先级：contradiction > conflict > duplicate > unreasonable
- 每条问题的 events 数组最多包含2个事件
- 仅基于事件本身的内容和世界观设定判定，不得凭空推断或主观推测
- 你的任务是检查问题，不是优化表达，不要对没有问题的部分提出优化建议

【输出格式】
只输出 JSON，不要输出任何开场白、解释或结束语。每条问题用 ════ITEM_START════ 和 ════ITEM_END════ 包裹。

发现问题：
════ITEM_START════
{{"type": "unreasonable|conflict|contradiction|duplicate", "events": [{{"id": 事件ID1, "title": "事件标题1"}}, {{"id": 事件ID2, "title": "事件标题2"}}], "description": "问题描述"}}
════ITEM_END════

- events 中的 id 必须与当前时间线事件列表中的 [ID:数字] 对应，title 为该事件的标题
- 字符串值中禁止使用未转义的双引号，如需引号请用单引号

没有问题：
════ITEM_START════
{{"type": "pass", "events": [], "description": "时间线整体合理，未发现问题。"}}
════ITEM_END════'''


# ==================== 根据描述生成事件字段 ====================

TIMELINE_GENERATE_FIELDS_PROMPT = '''
你是一位专业的小说时间线规划师。你的任务是根据用户输入的事件描述，补全并生成完整的时间线事件字段。

【输入】
小说标题：{project_title}
世界观设定摘要：
{outline}
用户输入的事件描述：
{description}
{context_section}

【职责】
根据事件描述和世界观设定，生成以下字段：

1. title：精炼的事件标题
   - 概括该事件的核心主题或时代特征
   - 风格应与世界观设定一致（如古风世界观用文言风格，科幻世界观用技术风格）

2. era_unit：纪年单位
   - 仅写纪年名称（如"炎兴"、"洪武"、"公元"），禁止包含"元年"或"年"后缀
   - 应与世界观设定中的纪年体系一致

3. start_year / start_month / end_year / end_month：时间范围
   - 纯数字（整数），0表示元年/未指定月份
   - 根据描述内容和世界观设定推断合理的时间范围
   - 时间范围应与事件的重要程度匹配：大事跨度长，小事跨度短

4. content：完善后的事件描述
   - 保留原文所有关键信息
   - 适当补充世界观设定中与该事件相关的背景信息（如地理位置、势力关系等），使描述更完整
   - 补充内容必须与原文描述直接相关，不得凭空编造新事件或新转折
   - 描述应为概述性质，2-4句话概括核心内容

【注意事项】
- 年份和月份必须是纯数字（整数），禁止输出"元年"等文字，0就写0
- 不得保留全0的时间范围，必须根据世界观和描述推断合理年月
- 如果有相邻事件上下文，生成的时间范围应与相邻事件保持逻辑连贯，不得与相邻事件时间冲突
- 字符串值中禁止使用未转义的双引号，如需引号请用单引号

【输出格式】
只输出一个 JSON 对象，不要输出任何开场白、解释或结束语。
{{"title": "事件标题", "era_unit": "纪年单位", "start_year": 0, "start_month": 0, "end_year": 0, "end_month": 0, "content": "完善后的事件描述"}}'''


TIMELINE_CHECK_OPTIMIZE_PROMPT = '''
你是一位专业的时间线修复编辑。请根据问题信息，调整存在问题的多个时间线事件。

小说标题：{project_title}

世界观设定摘要：
{outline}

涉及的事件及问题：
{events_section}

时间格式说明：事件时间使用结构化字段表示，如"纪元=炎兴, 起始年=0, 起始月=11, 结束年=0, 结束月=11"。其中起始年=0表示该纪元的元年（第一年），起始年=1表示该纪元的第二年，以此类推。同一纪元下，起始年=0和起始年=1是不同年份，0在前1在后。

用户的解决方案：{user_solution}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️ 修复前必读：核心约束（违反即失败）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
你的职责是调整涉及的事件，解决它们存在的问题。

如果用户提供了解决方案，严格按用户方案调整。
如果用户未提供解决方案（为空），则由你自行判断最佳修复方式。

必须遵守：
1. 用户提供了方案时，严格根据用户的解决方案进行调整，不要自行发挥
2. 用户未提供方案时，根据问题类型自行选择最合理的修复方式（如合并重复事件、调整时间范围消除冲突等）
3. 必须输出所有字段，未涉及的字段保持原值（包括纪元单位和所有时间字段）
4. 不得添加原文中未提及的新信息
5. 不得改变事件的核心含义（除非用户明确要求）
6. 每个涉及的事件都必须输出调整结果，即使某个事件不需要修改也要输出（保持原值）
7. 如果用户的解决方案是合并，则将指定的事件合并为一个
8. 如果用户的解决方案是调整时间，则相应修改时间范围
9. 年份和月份必须是纯数字（整数），禁止输出"元年"等文字，0就写0
10. era_unit字段只写纪年名称（如"炎兴"、"洪武"、"公元"），禁止包含"元年"、"年"等后缀

禁止行为：
- ❌ 无视用户的解决方案，自行决定如何修改
- ❌ 遗漏某个事件的输出
- ❌ 修改与问题无关的字段
- ❌ 遗漏任何字段（特别是时间字段：era_unit, start_year, start_month, end_year, end_month）
- ❌ era_unit中包含"元年"或"年"（如"炎兴元年"→正确应为"炎兴"）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【输出格式】
每个调整后的事件用 ════ITEM_START════ 和 ════ITEM_END════ 包裹：
════ITEM_START════
{{"match_title": "原事件标题", "title": "调整后的标题", "era_unit": "纪年单位", "start_year": 0, "start_month": 0, "end_year": 0, "end_month": 0, "description": "调整后的描述"}}
════ITEM_END════

如果用户要求合并两个事件为一个，则只输出一个 ITEM：
════ITEM_START════
{{"match_title": "事件A标题", "merge_with": "事件B标题", "title": "合并后的标题", "era_unit": "纪年单位", "start_year": 0, "start_month": 0, "end_year": 0, "end_month": 0, "description": "合并后的描述"}}
════ITEM_END════

⚠️ JSON格式约束（违反即失败）：
1. description字段中禁止使用未转义的双引号，如需引号请用单引号
2. 所有字符串值必须用双引号包裹，数字值不加引号
3. 只输出JSON，不要输出任何开场白、解释或结束语'''
