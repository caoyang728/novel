DESCRIPTION_ENHANCE_USER_PROMPT = '''请根据以下信息，完善小说描述，使其更加丰富、有吸引力、有深度。

书名：{title}

当前描述：
{description}
世界观内容(如果没有就忽略):
{worldview}
大纲内容(如果没有就忽略):
{outline}

请直接返回JSON格式，包含以下字段：
- title: 书名
- description: 描述

请只返回JSON，不要添加其他内容。

返回格式示例：
{{
    "title": "书名",
    "description": "描述内容"
}}'''

DESCRIPTION_ENHANCE_SYSTEM_PROMPT = '''你是一位专业的小说策划编辑，擅长为小说创作吸引人的描述。

请根据提供的信息，创作一个引人入胜的小说简介。
- 如果用户提供了书名，请参考并优化
- 如果用户没有提供书名，请根据描述生成一个吸引人的书名
- description 要求：约50-200字，语言精炼有力，突出核心亮点和爽点

请以JSON格式返回结果，返回格式示例：
{{
    "title": "书名",
    "description": "描述内容"
}}

请只返回JSON，不要添加其他内容。'''

# 书名建议提示词
TITLE_SUGGESTION_PROMPT = """请根据以下小说大纲，为小说提供几个合适的书名建议：

小说大纲：
{outline}

请提供3-5个书名建议，并简要说明每个书名的含义和亮点。

输出格式：
1. 书名1 - 说明
2. 书名2 - 说明
3. 书名3 - 说明
"""

TITLE_SUGGESTION_JSON_PROMPT = """请根据以下小说大纲，为小说提供5个吸引人的书名建议。

小说大纲：
{outline}

请以纯JSON数组格式返回，只包含书名，不要任何其他文字。

输出格式示例：
["书名1", "书名2", "书名3", "书名4", "书名5"]"""

TITLE_REWRITE_PROMPT = """请帮我修改以下书名，使其更有吸引力：

原书名：{original_title}

请以JSON数组格式返回5个改进后的书名建议。

输出格式示例：
["书名1", "书名2", "书名3", "书名4", "书名5"]
"""

# 描述优化提示词
DESCRIPTION_OPTIMIZE_PROMPT = """请帮我优化以下小说简介，使其更吸引人：

原简介：
{description}

请优化简介，使其：
1. 更吸引人，能抓住读者的注意力
2. 语言更流畅，表达更准确
3. 突出小说的核心卖点和亮点
4. 保持原文的核心内容和风格

请直接返回优化后的简介内容。"""

# 项目信息生成提示词
PROJECT_INFO_GENERATION_PROMPT = """请根据以下小说大纲，为小说生成多个吸引人的书名和一个简介：

小说大纲：
{outline}

请输出：
1. 书名：5个吸引人的小说书名
2. 简介：一段引人入胜的小说简介（约100字）

请以JSON格式返回，格式如下：
{{
  "titles": ["书名1", "书名2", "书名3", "书名4", "书名5"],
  "description": "小说简介"
}}"""
